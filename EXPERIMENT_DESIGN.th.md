# การออกแบบการทดลอง — ระบบค้นคืนกฎหมายไทยที่ใช้โครงสร้างของตัวบท

**ชื่อวิทยานิพนธ์ (ร่าง):** โครงสร้างของกฎหมายที่ผู้ร่างเขียนไว้ ช่วยให้ค้นหามาตราได้แม่นขึ้นจริงหรือไม่?
เปรียบเทียบการค้นคืนสามแบบบนประมวลกฎหมายอาญา ได้แก่ การไล่อ่านต้นไม้ตัวบทด้วย LLM (PageIndex),
การขยายผลด้วยกราฟความรู้ (KG) และการค้นแบบเวกเตอร์ผสม (dense-hybrid)

**สถานะ:** ตรึงการออกแบบไว้ก่อนลงมือเขียนโค้ด เอกสารนี้คือข้อตกลงที่ทั้งโค้ดและเนื้อวิทยานิพนธ์จะยึดตาม
ถ้าจะแก้ ให้แก้อย่างตั้งใจ ไม่ใช่แก้ไปเรื่อย ๆ แล้วลืมบันทึก

> เอกสารนี้เป็นฉบับภาษาไทยของ `EXPERIMENT_DESIGN.md` ถ้าสองฉบับเนื้อหาไม่ตรงกัน ให้ยึดฉบับอังกฤษเป็นหลัก
> แล้วปรับฉบับนี้ตาม

---

## 0. สรุปสั้น ๆ ว่าเราจะทำอะไร

เราใช้ประมวลกฎหมายอาญาเป็นคลังข้อมูลชุดเดียว แล้วตั้งคำถามง่าย ๆ ว่า:

> เมื่อผู้ใช้ถามคำถามกฎหมายมาหนึ่งข้อ วิธีไหนดึง "มาตราที่คำตอบต้องอ้างถึง" ออกมาได้แม่นที่สุด?

เราเทียบสามแนวทาง คือ (1) ใช้โครงสร้างที่ผู้ร่างกฎหมายเขียนไว้อยู่แล้ว — ภาค → ลักษณะ → หมวด → มาตรา —
โดยให้ LLM ไล่อ่านลงไปในต้นไม้ตัวบทแบบ PageIndex, (2) ใช้ความเชื่อมโยงทางกฎหมายที่ชัดเจน เช่น มาตรานี้
อ้างถึงมาตราไหน (CITES) หรือบทบัญญัติทั่วไปข้อไหนใช้กับความผิดข้อนี้ (APPLIES_TO) ผ่านกราฟความรู้ และ
(3) การค้นแบบเวกเตอร์ผสมที่แข็งแรง ซึ่งเป็นตัวตั้งเทียบ (baseline)

เราวัดสองอย่าง คือ คุณภาพการค้นคืนโดยตรง และผลที่ตามมาเมื่อเอามาตราที่ค้นได้ไปให้ **โมเดลภาษาไทยแบบ
โอเพนซอร์สตัวเดิม** สร้างคำตอบ ทั้งหมดวัดบนชุดข้อสอบทองคำชุดเดียวกัน และตรวจสอบด้วยวิธีทางสถิติที่จริงจัง

หัวใจของงานคือการ **แยกให้เห็นว่าแต่ละชั้นมีคุณค่าเท่าไร** — พูดง่าย ๆ คือ "โครงสร้างแต่ละส่วนช่วยเพิ่มคะแนน
Recall@5 กี่แต้ม และคะแนนนั้นส่งผลถึงคำตอบจริงหรือหายไประหว่างทาง"

---

## 1. คำถามวิจัยและสมมติฐาน

แต่ละคำถาม (RQ) ผูกกับแขนการทดลองใน §4 และตัวชี้วัดใน §6 สมมติฐานมีทิศทางชัดเจน และในทุกข้อ สมมติฐานว่าง
คือ "ไม่มีความแตกต่าง"

| ข้อ | เราถามว่า | วัดด้วย | เราคาดว่า |
|-----|----------|--------|----------|
| **RQ1** | PageIndex หามาตราที่ตอบคำถามโดยตรงได้ดีกว่า baseline หรือไม่ | Recall@5, MRR@10 เทียบ `gold_sections` | **H1**: โดยรวม PageIndex ไม่แพ้ baseline และได้เปรียบชัดที่สุดในคำถามที่ต้องอาศัยโครงสร้างของตัวบท เช่น บทนิยามและข้อยกเว้น |
| **RQ2** | การขยายผลด้วย KG ช่วยดึง "บทบัญญัติทั่วไป" ในภาค 1 ที่ต้องใช้ประกอบการตอบได้หรือไม่ | Multi-hop coverage@k เทียบ `gold ∪ supporting` | **H2**: KG ช่วยเพิ่มความครบถ้วนในคำถามแบบหลายมาตรา (multi-hop) โดยแทบไม่กระทบคำถามแบบหามาตราเดียว และตัวที่ช่วยจริงคือเส้นเชื่อม `APPLIES_TO` |
| **RQ3** | ค้นคืนแม่นขึ้นแล้ว คำตอบที่โมเดลสร้างดีขึ้นด้วยหรือไม่ | Claim-grounding rate, อัตราการอ้างมาตราลอย ๆ | **H3**: เมื่อใช้โมเดลตัวเดิม ค้นคืนยิ่งแม่น คำตอบยิ่งอิงหลักฐานจริงมากขึ้น |
| **RQ4** | โครงสร้างแต่ละชั้นแลกมาด้วยต้นทุนเท่าไร | จำนวนครั้งที่เรียก LLM ต่อคำถาม, เวลาตอบ (p95), เวลาสร้างดัชนี | **H4** (เชิงบรรยาย): PageIndex และ KG เพิ่มคุณภาพคำตอบ แต่แลกด้วยเวลาและต้นทุนที่พุ่งสูงขึ้นมาก เราวัดออกมาเป็นตัวเลข |
| **RQ5** | ในเมื่อประมวลกฎหมายอาญาน่าจะอยู่ในข้อมูลที่ LLM เคยเรียนมาแล้ว การค้นคืนยังจำเป็นแค่ไหน | ผลต่างระหว่างแขนที่ค้นคืน กับแขนที่ไม่ให้ดูตัวบทเลย | **H5**: การค้นคืนยังเพิ่มคุณค่าเหนือความจำของโมเดล โดยเฉพาะเรื่องตัวเลขโทษและมาตราที่แทรกเพิ่มภายหลัง (ทวิ/ตรี) ที่โมเดลมักจำผิด |

RQ5 ทำหน้าที่กันปัญหา "โมเดลจำคำตอบมาจากตอนเทรน" และสำหรับวิทยานิพนธ์กฎหมายไทย ลำพังคำถามนี้ก็ถือเป็น
ประเด็นที่น่าสนใจในตัวเอง

---

## 2. รูปแบบการออกแบบ

- **ทุกคำถามเจอทุกแขนเหมือนกัน (จับคู่กัน)** ข้อสอบทุกข้อถูกส่งผ่านทุกแขนการทดลอง วิธีนี้ตัดปัญหา
  "บางแขนบังเอิญเจอคำถามง่าย" ออกไป และทำให้การทดสอบทางสถิติมีพลังมากกว่าการแบ่งคำถามคนละชุด ซึ่งสำคัญมาก
  เพราะเรามีกฎหมายแค่ฉบับเดียวและจำนวนข้อสอบมีจำกัด
- **แกนหลักเป็นตาราง 2×2** เพื่อดูว่าองค์ประกอบสองอย่างช่วยแยกกันหรือช่วยเสริมกัน:
  - **แกน A — วิธีค้นฐาน:** `hybrid-baseline` กับ `pageindex`
  - **แกน B — การขยายด้วย KG:** เปิด กับ ปิด
- **แขนอ้างอิง** อีกสามแบบ (ไม่อยู่ในตาราง 2×2) ทำหน้าที่เป็นกรอบบน-ล่างของผล ได้แก่ แบบไม่ให้ดูตัวบทเลย
  (ขอบล่าง), แบบป้อนเฉลยให้ตรง ๆ (ขอบบน) และ BM25 ล้วน (พื้นของการค้นเชิงคำ)
- **ใช้โมเดลสร้างคำตอบตัวเดียวตลอด** สำหรับการเทียบหลัก ส่วนโมเดลไทยตัวที่สองเก็บไว้รันซ้ำเพื่อตรวจว่า
  ข้อสรุปไม่ได้ขึ้นกับโมเดล (§9)

---

## 3. คลังข้อมูลและไฟล์ที่มีอยู่แล้ว

โครงสร้างพื้นฐานสร้างไว้แล้วในโครงร่างโปรเจกต์:

| ไฟล์ | สร้างโดย | ใช้ทำอะไรในการทดลอง |
|------|---------|---------------------|
| `penal_tree.json` | `ingest/pageindex_parser.py` | ต้นไม้ตัวบทที่แขน PageIndex ใช้ไล่อ่าน |
| `sections.jsonl` (1 บรรทัด = 1 มาตรา) | ตัวแยกวิเคราะห์ตัวเดียวกัน | หน่วยข้อมูลของ baseline, แหล่งโหนดของ KG, และดัชนีสำหรับค้นคืน |
| `kg_nodes/edges.jsonl`, `kg.cypher` | `kg/build_kg.py` | เส้นเชื่อม CITES / HAS_PENALTY / APPLIES_TO (ข้อหลังนักกฎหมายคัดเอง) |
| `eval/eval_set.jsonl` | การกำกับข้อสอบ (§5) | ชุดเฉลยทองคำที่ทุกแขนถูกวัดด้วยชุดเดียวกัน |

**เรื่องเวอร์ชันของกฎหมาย (กฎหมายไม่มีผลย้อนหลัง):** ให้เลือกฉบับแก้ไขเพิ่มเติมมาหนึ่งฉบับ ตรึงไว้ บันทึกวันที่
มีผลบังคับใช้ และประทับวันที่นั้น (`applicable_version_date`) ลงในข้อสอบทุกข้อ เพราะจำนวนโทษและมาตราที่แทรก
เพิ่มต่างกันไปในแต่ละฉบับ ถ้าใช้เวอร์ชันผิด คำถามประเภทโทษ (penalty) จะผิดทั้งหมดโดยที่เราไม่รู้ตัว นี่คือเรื่อง
ความถูกต้องของงาน ไม่ใช่รายละเอียดปลีกย่อย

---

## 4. แขนการทดลอง

ทุกแขนตัดข้อมูลเป็นหน่วยเดียวกันคือ **หนึ่งมาตราต่อหนึ่งชิ้น** ต่างกันแค่ "วิธีเลือกมาตรา" ไม่ใช่ความละเอียด
ของการตัด เพื่อไม่ให้มีตัวแปรอื่นมาปน

### 4a. สี่แขนในตาราง 2×2 (แขนที่เราใช้สรุปผลจริง)

| รหัส | วิธีค้นฐาน | KG | คำอธิบาย |
|------|----------|----|---------|
| **A1** `HYB` | เวกเตอร์ผสม | ปิด | **แขนควบคุม** ค้นด้วย BGE-M3 ทั้งแบบ dense และ sparse รวมผลกัน แล้วให้ reranker (cross-encoder) จัดอันดับใหม่ |
| **A2** `HYB+KG` | เวกเตอร์ผสม | เปิด | เอา top-k ของ A1 เป็นจุดตั้งต้น แล้วขยายไปตามเส้น CITES/APPLIES_TO อีกหนึ่งกระโดด จากนั้นจัดอันดับใหม่ |
| **A3** `PI` | PageIndex | ปิด | ให้ LLM ไล่อ่านต้นไม้ `penal_tree.json` จากภาคลงไปจนถึงมาตรา แล้วคืนมาตราที่เลือก |
| **A4** `PI+KG` | PageIndex | เปิด | เอามาตราที่ PageIndex เลือก มาขยายด้วย KG แล้วจัดอันดับใหม่ |

### 4b. แขนอ้างอิง (ไว้เป็นกรอบเทียบ)

| รหัส | แขน | มีไว้ทำไม |
|------|-----|----------|
| **R0** `BM25` | ค้นด้วยคำล้วน | เป็นพื้นล่าง ดูว่าการค้นแบบเวกเตอร์ยกคะแนนขึ้นเท่าไร |
| **R1** `DENSE` | BGE-M3 อย่างเดียว ไม่มี reranker | ดูว่า reranker ในแขน baseline ช่วยเท่าไร |
| **C0** `CLOSED` | ให้โมเดลตอบ **โดยไม่ให้ดูตัวบทเลย** | วัดว่าโมเดลจำอะไรมาได้เอง เป็นตัวหารของ RQ5 |
| **C1** `ORACLE` | ป้อนเฉลย (`gold ∪ supporting`) ให้ตรง ๆ | เป็นเพดานของการสร้างคำตอบ ช่วยแยกว่า "ค้นไม่เจอ" หรือ "โมเดลตอบไม่เก่ง" |

`CLOSED` กับ `ORACLE` เป็นแขนที่วัดเฉพาะการสร้างคำตอบ (ไม่มีคะแนนค้นคืน) ส่วนแขนอื่นวัดทั้งค้นคืนและสร้างคำตอบ

### 4c. กติกาเพื่อให้เทียบกันอย่างเป็นธรรม (ตกลงไว้ล่วงหน้า)

- ทุกแขน (A1–A4, C0, C1) ใช้ **โมเดลสร้างคำตอบตัวเดียวกัน เทมเพลตพรอมป์เดียวกัน และตั้ง temperature 0**
- ทุกแขน **ป้อนมาตราให้โมเดลจำนวน k เท่ากัน** (ค่าเริ่มต้น k=5 และจะลองปรับใน §8) แม้ KG จะจัดอันดับผู้เข้าชิง
  มากกว่า แต่สุดท้ายโมเดลก็เห็นแค่ top-k หลังจัดอันดับใหม่
- **ตัวไล่อ่านต้นไม้ของ PageIndex ใช้โมเดลไทยตัวเดียวกับที่สร้างคำตอบ** ถ้าไปใช้โมเดลที่เก่งกว่าอย่าง Claude
  มาไล่อ่าน เราจะแยกไม่ออกว่า "โครงสร้างช่วย" หรือ "โมเดลเก่งกว่าช่วย" และเราจะนับจำนวนครั้งที่มันเรียก LLM
  เข้าไปในต้นทุนด้วย (RQ4)
- **ใช้ reranker ตัวเดียวกันทุกที่ที่มีการจัดอันดับใหม่** เพื่อไม่ให้แขนใดได้เปรียบเพราะบังเอิญใช้ reranker คนละตัว

---

## 5. การสร้างชุดข้อสอบ

ทำตาม `eval/ANNOTATION_GUIDE.md` และ `eval/schema.json` หัวข้อนี้กำหนดตัวเลขให้ชัด

- **จำนวน:** เริ่มนำร่อง 50 ข้อ (ครบทุกประเภท เพื่อไล่บั๊กระบบให้จบก่อน) แล้วค่อยขยายเป็น **อย่างน้อย 250 ข้อ**
- **สัดส่วนประเภทคำถาม** (ตามคู่มือ): หามาตราเดียว 25% / หลายมาตรา 30% / โทษ 20% / บทนิยาม 10% /
  ข้อยกเว้น 10% / ตอบไม่ได้ 5% เราตั้งใจให้คำถามแบบหลายมาตรามีสัดส่วนสูง เพราะเป็นจุดชี้ขาด H1/H2
  ที่ 250 ข้อ จะได้ราว 75 ข้อ ซึ่งเป็นกลุ่มที่จำกัดพลังการทดสอบทางสถิติ (§7)
- **ผู้กำกับสองคน ให้ทำข้อซ้ำกัน 20%** แล้วรายงานความสอดคล้องระหว่างสองคนบน `gold_sections` ด้วยค่า Jaccard
  (เทียบชุดมาตรา) และ Cohen's κ (เทียบการตัดสินว่ามาตราไหนเป็นเฉลย) ตั้งเป้า κ ≥ 0.7 ก่อนจะเชื่อชุดข้อสอบ
  ถ้าไม่ตรงกันให้มาตัดสินร่วมกัน
- **ตรวจความถูกต้องอัตโนมัติ:** ให้ `eval/validate_eval.py` รันใน CI คอยเช็กว่าเลขมาตราที่อ้างเป็นเฉลยมีอยู่จริง
  ใน `sections.jsonl`, ข้อที่ตอบไม่ได้ต้องไม่มีเฉลย และประเภทคำถามต้องถูกต้อง
- **ระวังข้อตัวอย่าง:** ข้อใน `eval_set.seed.jsonl` เป็นแค่ตัวอย่าง ก่อนเอาเข้าชุดจริงต้องตรวจเลขมาตราและจำนวน
  โทษทุกข้อกับฉบับกฎหมายที่เราตรึงไว้อีกครั้ง

---

## 6. ตัวชี้วัด

### 6a. การค้นคืน (ให้คะแนนทีละข้อแล้วเฉลี่ย)

กำหนดให้ `R_k` คือมาตรา top-k ที่แขนนั้นจัดอันดับมา, `G` คือ `gold_sections`, และ `G⁺` คือ `gold ∪ supporting`

| ตัวชี้วัด | หมายถึง | ตอบคำถามว่า |
|---------|--------|------------|
| **Recall@k** | สัดส่วนของมาตราเฉลยที่อยู่ใน top-k (k = 1,3,5,10) | เราดึงมาตราที่ตอบคำถามขึ้นมาได้ไหม (RQ1) |
| **MRR@10** | ค่าเฉลี่ยของ 1 หารด้วยอันดับของมาตราเฉลยตัวแรกที่เจอ | มาตราที่ใช่ถูกจัดอันดับสูงแค่ไหน (RQ1) |
| **MAP@10** | ความแม่นยำเฉลี่ยเมื่อมีเฉลยหลายมาตรา | คุณภาพการจัดอันดับโดยรวม |
| **Multi-hop coverage@k** | ได้ 1 ถ้ามาตราที่ต้องใช้ทั้งหมด (`G⁺`) อยู่ครบใน top-k ไม่งั้นได้ 0 แล้วเฉลี่ย | ดึงมาตราครบทุกชิ้นที่ต้องใช้ตอบไหม (RQ2) |
| **Support recall@k** | Recall เฉพาะมาตรา `supporting` | แยกดูผลของ KG กับบทบัญญัติทั่วไปโดยเฉพาะ (RQ2) |

ทุกตัวรายงานทั้ง **ค่ารวม และแยกตามประเภทคำถาม** การแยกตามประเภทสำคัญมาก เพราะ H1/H2 ซ่อนอยู่ตรงนั้น
ถ้าดูแต่ค่ารวมอาจมองไม่เห็น

### 6b. การสร้างคำตอบ (ใช้โมเดลตัวเดิม + ตรวจด้วย LLM-as-judge และการอิงหลักฐาน)

| ตัวชี้วัด | หมายถึง | ตอบคำถามว่า |
|---------|--------|------------|
| **Claim-grounding rate** | สัดส่วนของประเด็นย่อยในคำตอบ (`answer_claims`) ที่มีมาตราที่ค้นมารองรับจริง โดยให้ LLM ตัดสินทีละประเด็น | คำตอบอิงหลักฐานจริงแค่ไหน จับกรณี "อ้างมาตราถูกแต่บอกโทษผิด" ได้ |
| **Answer correctness** | ให้ LLM เทียบคำตอบกับเฉลย `reference_answer` ตามเกณฑ์ (ถูก/ผิด หรือ 1–5) | คำตอบถูกไหม |
| **อัตราการอ้างมาตราลอย ๆ** | สัดส่วนคำตอบที่อ้างมาตราซึ่งไม่มีอยู่ในกฎหมาย (ตรวจด้วย regex ได้เลย) หรือไม่ได้ถูกค้นมา | โมเดลกุมาตราขึ้นมาเองบ่อยแค่ไหน |
| **ความแม่นของการปฏิเสธ** | สำหรับคำถามที่ตอบไม่ได้ วัด precision/recall/F1 ของการปฏิเสธอย่างถูกต้อง | ระบบยอมบอกว่า "ตอบไม่ได้" แทนการมั่วไหม |

**วิธีใช้ LLM มาเป็นผู้ตัดสิน (ตกลงไว้ล่วงหน้าเพื่อให้ปกป้องได้):**
- โมเดลผู้ตัดสิน **ต้องไม่ใช่โมเดลตัวเดียวกับที่สร้างคำตอบ** เพื่อกันการเข้าข้างตัวเอง ตั้ง temperature 0 และ
  ใช้พรอมป์ตายตัว
- การตรวจว่าคำตอบอิงหลักฐานไหม ทำทีละประเด็นแบบตอบใช่/ไม่ โดยเทียบกับมาตราที่ค้นมาต่อกัน วิธีนี้รวมคะแนน
  ได้ผลแน่นอน และ `answer_claims` ที่ซอยเป็นประเด็นย่อยไว้แล้วก็เหมาะกับการตรวจแบบนี้พอดี
- **ต้องตรวจสอบตัวผู้ตัดสินเองด้วย:** ให้นักกฎหมายสุ่มตรวจคำถาม-คำตอบราว 20% แล้วดูว่าผู้ตัดสินที่เป็น LLM
  ให้คะแนนตรงกับคนแค่ไหน (κ หรือ Spearman) เราจะเชื่อคะแนนอัตโนมัติก็ต่อเมื่อมันตรงกับคนมากพอ และจะรายงาน
  ความตรงนี้ในเล่มเสมอ นี่เป็นจุดเดียวที่เราให้คนมาให้คะแนน ไม่ได้ทำเพื่อตรวจทั้งชุด แต่ทำเพื่อยืนยันว่าใช้ LLM
  ตัดสินแทนคนได้

### 6c. ต้นทุนของระบบ (RQ4)

วัดเวลาตอบ p50/p95 โดย **แยกส่วนค้นคืนกับส่วนสร้างคำตอบ**, นับจำนวนครั้งที่เรียก LLM ต่อคำถาม (การไล่อ่านของ
PageIndex และการจัดอันดับด้วย KG กินการเรียกจริง นี่คือจุดที่โครงสร้างเริ่มแพง), นับจำนวนโทเคน, เวลาสร้างดัชนี
และพื้นที่เก็บดัชนี

---

## 7. วิธีทางสถิติ

- **หน่วยที่วิเคราะห์คือข้อสอบแต่ละข้อ** (จับคู่กันข้ามทุกแขน)
- **แบบจำลองหลักที่ใช้สรุปผล — mixed-effects logistic regression** สำหรับตาราง 2×2 ใช้กับผลแบบได้/ไม่ได้
  รายข้อ เช่น เจอเฉลยใน top-5 ไหม:
  `outcome ~ base_retriever * kg_expansion + (1 | item)`
  ตัว `(1 | item)` คือการบอกแบบจำลองว่าข้อเดียวกันถูกวัดซ้ำในหลายแขน (คือการจับคู่) ส่วนเทอมคูณ (interaction)
  ตอบคำถามตรง ๆ ว่า "KG ช่วยตอน PageIndex มากกว่าตอน hybrid ไหม" ส่วนตัวชี้วัดที่เป็นสัดส่วน เช่น
  claim-grounding ใช้แบบจำลองผสมสำหรับค่าต่อเนื่อง หรือเฉลี่ยก่อนแล้วทำ bootstrap ก็ได้
- **ช่วงความเชื่อมั่น:** ใช้ paired bootstrap หาช่วงความเชื่อมั่น 95% (สุ่มซ้ำ 10,000 รอบจากข้อสอบ) ให้กับทุก
  ตัวชี้วัดหลักและทุกผลต่างระหว่างคู่แขน รายงานผลต่างพร้อมช่วงความเชื่อมั่น ไม่ใช่รายงานแค่ค่า p
- **การทดสอบนัยสำคัญรายคู่:** ใช้ paired permutation test (หรือ McNemar สำหรับผลแบบได้/ไม่ได้) กับคู่สำคัญ
  ได้แก่ A3 กับ A1 (RQ1), A2 กับ A1 และ A4 กับ A3 (RQ2), และแต่ละแขนที่ค้นคืนเทียบกับ C0 (RQ5)
- **ปรับค่าการทดสอบหลายครั้ง:** ใช้ Holm–Bonferroni กับชุดการทดสอบทั้งหมดที่ตกลงไว้ล่วงหน้า
- **รายงานขนาดของผลด้วย** (ต่างกันกี่แต้ม, odds ratio) ควบคู่กับค่า p เพราะวิทยานิพนธ์ต้องเถียงเรื่องขนาดของผล
  ไม่ใช่แค่ว่ามีนัยสำคัญหรือไม่
- **เหตุผลเรื่องจำนวนข้อสอบ:** ด้วยการออกแบบแบบจับคู่ ถ้าต้องการจับความต่างของ Recall@5 ราว 8–10 แต้ม
  ที่พลัง 80% และ α=0.05 ต้องใช้ราว 150–200 ข้อโดยรวมก็พอ แต่กลุ่มคำถามหลายมาตรา (~75 ข้อ) เป็นตัวจำกัด
  จับได้เฉพาะผลที่ต่างกันราว 15 แต้มขึ้นไป ต้องระบุข้อจำกัดนี้ไว้แต่แรก และอย่าเคลมความต่างเล็ก ๆ ในกลุ่มย่อยเกินจริง

---

## 8. การทดลองแยกส่วน (Ablation) — ไล่ดูว่าอะไรช่วยจริง

แต่ละอย่างแยกทดสอบทีละปัจจัย เพื่อกันคำถามจากกรรมการว่า "แน่ใจได้ยังไงว่าเป็นเพราะสิ่งนั้นจริง"

1. **ลองค่า k หลายค่า:** k = 1, 3, 5, 10 กับทุกแขน เพื่อดูเส้นแลกเปลี่ยนระหว่างค้นคืนกับสร้างคำตอบ
2. **แยกทดสอบเส้นเชื่อมของ KG:** ใช้ CITES อย่างเดียว → เพิ่ม APPLIES_TO → เพิ่ม HAS_PENALTY เพื่อชี้ให้ชัดว่า
   เส้นไหนคือตัวช่วยเรื่องหลายมาตรา (H2 เดาว่าเป็น APPLIES_TO)
3. **เปิด/ปิด reranker ใน baseline** (A1 เทียบ R1) เพื่อดูว่าคะแนนมาจาก BGE-M3 เท่าไร มาจาก reranker เท่าไร
4. **ปรับความลึกของการไล่อ่าน PageIndex:** จำกัดจำนวนก้าวที่ให้ไล่อ่าน แล้วดูว่ายิ่งให้ LLM คิดมากขึ้นยังคุ้มไหม
5. **ทดสอบว่าผลไม่ขึ้นกับโมเดล:** รัน A1–A4 ซ้ำด้วยโมเดลไทยตัวที่สอง แล้วดูว่า *อันดับ* ของแขนยังเหมือนเดิมไหม
   แม้คะแนนดิบจะขยับ (ยืนยันว่าข้อสรุปใช้ได้กว้าง ไม่ใช่บังเอิญกับโมเดลตัวเดียว)

---

## 9. โมเดลที่ใช้ (โอเพนซอร์สภาษาไทย ตามที่เลือกไว้)

| หน้าที่ | ตัวเลือก | หมายเหตุ |
|--------|---------|---------|
| สร้างคำตอบ (หลัก) | Typhoon / SeaLLM / OpenThaiGPT — เลือกหนึ่งตัว ตรึงเวอร์ชันให้แน่นอน | ปรับแต่งภาษาไทยมาแล้ว โฮสต์เองได้ ทำซ้ำได้ ใช้เป็นตัวหลักตลอด A1–A4/C0/C1 |
| สร้างคำตอบ (ทดสอบความคงทน) | อีกตัวจากรายการข้างบน | ใช้ใน ablation ข้อ §8.5 |
| ไล่อ่านต้นไม้ PageIndex | ตัวเดียวกับที่สร้างคำตอบหลัก | กันไม่ให้ "โครงสร้างช่วย" กลายเป็น "โมเดลใหญ่กว่าช่วย" |
| ตัวฝังเวกเตอร์ (embedding) | BGE-M3 (dense + sparse) | แกนหลักของ baseline รองรับหลายภาษา ทำงานกับภาษาไทยได้ดี |
| Reranker | cross-encoder หลายภาษา เช่น BGE-reranker-v2-m3 | ใช้ร่วมกันทุกแขนที่มีการจัดอันดับใหม่ |
| ผู้ตัดสิน (judge) | โมเดลคนละตัวกับที่สร้างคำตอบ | กันการเข้าข้างตัวเอง และตรวจสอบเทียบกับคน (§6b) |

*(เวอร์ชันและการบีบอัดโมเดลที่แน่นอนจะระบุในภาคผนวกตอนลงมือทำ ในขั้นออกแบบขอแค่กำหนดว่าต้องตรึงและรายงานให้ครบ)*

---

## 10. จุดเสี่ยงที่จะทำให้ผลไม่น่าเชื่อ และวิธีรับมือ

| จุดเสี่ยง | วิธีรับมือ |
|----------|----------|
| **โมเดลอาจจำคำตอบมาจากตอนเทรน** เพราะประมวลกฎหมายเป็นข้อมูลสาธารณะ | มีแขน `CLOSED` (C0) วัดว่าโมเดลจำได้เองแค่ไหน แล้วรายงานผลการค้นคืนแบบหักส่วนนี้ออก (RQ5) คำถามเรื่องโทษและมาตราแทรก (ทวิ) จะเน้นกรณีที่โมเดลมักจำผิด |
| **ใช้กฎหมายฉบับเดียว ผลอาจใช้ไม่ได้กว้าง** | วางกรอบตรง ๆ ว่าเป็นการศึกษาบนประมวลกฎหมายอาญาไทย และ §8.5 ที่สลับโมเดลช่วยยืนยันว่าข้อสรุปมั่นคง ส่วนการนำไปใช้กับกฎหมายฉบับอื่นยกไว้เป็นงานต่อยอด |
| **LLM ผู้ตัดสินอาจมีอคติหรือให้คะแนนมั่ว** | ใช้ผู้ตัดสินคนละตัวกับตัวสร้างคำตอบ, temperature 0, ตรวจทีละประเด็น และปรับเทียบกับคน (§6b) |
| **`APPLIES_TO` คัดโดยนักกฎหมายคนเดียว** | ถือเป็นทรัพยากรที่กำกับไว้ ให้คนที่สองสุ่มตรวจ และ ablation §8.2 จะบอกเองว่าผลพึ่งพาเส้นนี้แค่ไหน |
| **ข้อสอบตัวอย่างผิด หรือใช้ฉบับกฎหมายผิด** | ตรึงเวอร์ชันกฎหมาย + ให้ `validate_eval.py` รันใน CI + ตรวจเลขทุกข้อซ้ำ |
| **เลขไทยและการตัดคำ** (๓๓๕/๑, ทวิ) | ตัวแยกและ KG แปลงเลขมาตราเป็นรูปแบบมาตรฐานอยู่แล้ว เพิ่ม unit test ยืนยันว่า id ตรงกันทั้งฝั่งตัวแยก KG และเฉลย |
| **reranker เป็นตัวแปรแอบแฝง** | ใช้ reranker ตัวเดียวกันทุกแขนที่จัดอันดับใหม่ และมี ablation เปิด/ปิด (§8.3) |

---

## 11. การทำซ้ำได้

- ตรึงให้หมด: เวอร์ชันโมเดล, การบีบอัดโมเดล, เวอร์ชัน embedding และ reranker, ค่าถอดรหัส (temperature 0),
  ค่า k, seed สุ่ม และวันที่ฉบับกฎหมาย
- เก็บเวอร์ชันและเผยแพร่: ชุดข้อสอบ `eval_set.jsonl`, พรอมป์ทั้งหมด (ไล่อ่าน/สร้างคำตอบ/ตัดสิน), ไฟล์กฎหมายที่ตรึง
  และไฟล์ตั้งค่าการรัน
- ทุกตัวเลขในเล่มต้องสร้างซ้ำได้ด้วยคำสั่งเดียวจากไฟล์ตั้งค่าที่ตรึงไว้
- เก็บผลดิบรายข้อ (มาตราที่ค้นได้, คำตอบที่สร้าง, คำตัดสินของผู้ตัดสิน) เพื่อคำนวณตัวชี้วัดใหม่ได้โดยไม่ต้องรันโมเดลซ้ำ

---

## 12. แผนทำงานเป็นระยะ

| ระยะ | ส่งมอบอะไร | ถือว่าผ่านเมื่อ |
|------|-----------|---------------|
| **P0. ตรึงคลังข้อมูล** | แยกวิเคราะห์กฎหมายทั้งฉบับ ได้ `sections.jsonl` + `penal_tree.json` ประทับวันที่เวอร์ชัน | ตัวแยกครบทุกมาตราและ id แปลงไป-กลับได้ผ่าน test |
| **P1. ข้อสอบนำร่อง** | 50 ข้อ ครบทุกประเภท ตรวจแล้ว | `validate_eval.py` ผ่าน และรันครบวงจรได้หนึ่งแขน |
| **P2. ตั้ง baseline** | R0/R1/A1 ฝั่งค้นคืน + C0/C1 ฝั่งสร้างคำตอบ | คำนวณตัวชี้วัด + bootstrap ซ้ำได้บนชุดนำร่อง |
| **P3. แขนเชิงโครงสร้าง** | A3 (PageIndex), A2/A4 (KG) และคัด `APPLIES_TO` | รันตาราง 2×2 บนชุดนำร่องได้ ผลสมเหตุสมผลเทียบ baseline |
| **P4. ขยายข้อสอบ** | อย่างน้อย 250 ข้อ ทำซ้ำ 20% รายงานความสอดคล้อง | κ ≥ 0.7 และสัดส่วนประเภทตรงเป้า |
| **P5. รันเต็ม** | ทุกแขน × ข้อสอบเต็มชุด + ablation §8 | คำนวณครบทุกการเทียบที่ตกลงไว้ |
| **P6. ตรวจสอบผู้ตัดสิน** | ให้คนตรวจ 20% เทียบกับ LLM ผู้ตัดสิน | รายงานความตรงกับคน และยืนยันว่าใช้คะแนนอัตโนมัติได้ |
| **P7. วิเคราะห์และเขียนเล่ม** | ผลจากแบบจำลอง + bootstrap แยกตามประเภท และเส้นต้นทุน | ตอบ RQ1–RQ5 ได้ พร้อมขนาดผลและช่วงความเชื่อมั่น |

---

## 13. งานนี้ถือว่าสำเร็จเมื่อไร (ตกลงเกณฑ์ไว้ก่อน)

จุดแข็งของการออกแบบนี้คือ **ไม่ว่าผลจะออกทางไหนก็เป็นวิทยานิพนธ์ที่ดีได้** เพราะเราแยกคุณค่าของแต่ละชั้นออกมา
เป็นตัวเลข:

- ถ้า **H1/H2 เป็นจริง** แปลว่าการค้นคืนที่ใช้โครงสร้างเอาชนะ baseline เวกเตอร์ผสมภาษาไทยที่แข็งแรงได้จริง
  ในงานตอบคำถามกฎหมาย และได้เปรียบเฉพาะประเภทคำถามที่ทฤษฎีคาดไว้ ตรงตามที่ควรจะเป็น
- ถ้า **ไม่จริง** ก็ยังเป็นผลลัพธ์ที่มีค่า — คือ "พอมีตัวค้นแบบเวกเตอร์ผสมที่แข็งแรงและตัดข้อมูลเป็นรายมาตราดี ๆ แล้ว
  โครงสร้างของตัวบทช่วยเพิ่มได้น้อย" ซึ่งเป็นผลเชิงลบที่ตีพิมพ์และปกป้องได้ และแขน CLOSED/ORACLE ก็อธิบายได้ว่า
  ทำไมถึงเป็นอย่างนั้น

การกำหนดแขน ตัวชี้วัด และการทดสอบไว้ล่วงหน้าอย่างในเอกสารนี้ คือสิ่งที่ทำให้ผลลัพธ์ทั้งสองทางเป็นงานวิจัยที่หนักแน่น
ไม่ใช่แค่ความรู้สึก
```
